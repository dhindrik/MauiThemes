<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiThemes.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiThemes"
    Shell.BackgroundColor="{DynamicResource BackgroundColor}"
    Shell.TitleColor="{DynamicResource TextColor}"
    Shell.FlyoutBehavior="Disabled">
    <TabBar>
        <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

        <ShellContent
        Title="Custom"
        ContentTemplate="{DataTemplate local:CustomPage}"
        Route="CustomPage" />
    </TabBar>
</Shell>

